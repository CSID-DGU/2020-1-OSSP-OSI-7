package api

import (
	"oss/web"
)

func initPublicRouters(context *web.Context) {
	//context.Engine.GET("/swagger/*any", ginSwagger.WrapHandler(swaggerFiles.Handler))
	//context.Engine.POST("/", service.CreateClass(context))
}